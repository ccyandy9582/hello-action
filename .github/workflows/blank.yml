# This is a basic workflow to help you get started with Actions

name: enhancement of xxxx

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  # workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  # deploy:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest

  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2

  #     # Runs a single command using the runners shell
  #     - name: check xxx
  #       run: echo "xxx is good"

  #     # Runs a set of commands using the runners shell
  #     - name: check yyy
  #       run: |
  #         echo "check yyy"
  #         echo "yyy is good"
  # rollback:
  #   runs-on: ubuntu-latest
  #   needs: deploy
  #   steps:
  #     - name: prepear rollback
  #       run: |
  #         echo "start rollback"
  #         echo "rollbacking..."
  #         echo "rollback successfully"
  # runScript:
  #   runs-on: ubuntu-latest
  #   steps: 
  #     - uses: actions/checkout@v2
  #     - name: list dir
  #       run: |
  #         ls -al
  #     - name: run script
  #       run: |
  #         chmod +x ./.github/scripts/helloworld.sh
  #         ./.github/scripts/helloworld.sh
  #       shell: bash
  #     - name: set message
  #       run: |
  #         echo "::debug::Set the Octocat variable"
  #     - name: get message
  #       run: |
  #         echo "please download and look into logs"
  runJava:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '11'
          java-package: jre
      - run: java -cp java ./java/Hello