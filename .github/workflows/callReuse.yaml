name: Call a reusable workflow

on:
  workflow_dispatch:

jobs:
  call-workflow:
    uses: ./.github/workflows/reuse.yaml@main

  call-workflow-passing-data:
    uses: ./.github/workflows/reuseWithParam.yaml@main
    with:
      username: mona1234